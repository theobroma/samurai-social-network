import React from 'react';

interface Props {
  // any
}

export const ProfileDataForm: React.FC<Props> = (props) => {
  return (
    <div>
      <div>ProfileDataForm</div>
    </div>
  );
};

export default ProfileDataForm;
